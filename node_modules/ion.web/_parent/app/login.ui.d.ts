/// <reference path="core.ui.d.ts" />
declare module ionweb {
}
declare namespace ionweb.auth {
}
declare namespace ionweb {
    /**
     * Local SDK configuration, section 'shell'. Can be overridden by ION.WEB server.
     */
    interface IShellConfig {
        /**
         * Link label in LoginDialog, by default iongroup.com
         */
        loginDialogLinkLabel?: string;
        /**
         * URL to define a secondary branding logo
         */
        secondaryBrandingLogoUrl?: string;
        /**
         * This flag will show OTP at login and change password screen by default. Default value of the flag is false
         * It accepts either boolean or a list of clientIDs for which OTP should be enabled.
         */
        enableOtpAuthentication?: boolean | string[];
        /**
         * This flag will make the OTP optional. If set to true, it will allow to login without OTP. Default value of the flag is false
         * It accepts either boolean or a list of clientIDs for which OTP should be optional.
         */
        allowEmptyOtp?: boolean | string[];
    }
}
declare namespace pako {
}
